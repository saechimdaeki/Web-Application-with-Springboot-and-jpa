spring:

  # springboot에서 별도의 설정이 없으면 memeory에서 돌리므로 다 없애도가능능
# datasource:
  #  url: jdbc:h2:mem:test  #h2 인메모리
   # username: sa
   # password:
   # driver-class-name: org.h2.Driver


#  jpa:
#    hibernate:
#      ddl-auto: create-drop  //springboot 기본설정은 creat-drop
  ## create는 가지고있는 entity다 드랍하고 create한뒤 애플리케이션 실행  create-drop은 마지막에
  ## 어플리케이션 종료시점에 drop쿼리 날림


#    properties:
#      hibernate:
#      show_sql: true
#    format_sql: true
  logging:
    level:
      org.hibernate.SQL: debug
      org.hibernate.type: trace
